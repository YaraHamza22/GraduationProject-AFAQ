import AuditorWorkspace from "@/features/auditor/pages/AuditorWorkspace";

export const metadata = {
  title: "Auditor Profile | Afaq",
};

export default function AuditorProfilePage() {
  return <AuditorWorkspace mode="profile" />;
}
