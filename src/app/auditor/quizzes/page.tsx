import AuditorWorkspace from "@/features/auditor/pages/AuditorWorkspace";

export const metadata = {
  title: "Quiz Audit | Afaq",
};

export default function AuditorQuizzesPage() {
  return <AuditorWorkspace mode="quizzes" />;
}
