import AuditorWorkspace from "@/features/auditor/pages/AuditorWorkspace";

export const metadata = {
  title: "Content Review | Afaq",
};

export default function AuditorCoursesPage() {
  return <AuditorWorkspace mode="courses" />;
}
